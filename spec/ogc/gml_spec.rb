# frozen_string_literal: true

RSpec.describe Ogc::Gml do
  it "has a version number" do
    expect(Ogc::Gml::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
